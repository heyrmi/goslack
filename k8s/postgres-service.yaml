apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: goslack
  labels:
    app: postgres
spec:
  type: ClusterIP  # Internal service - not exposed outside the cluster
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres

